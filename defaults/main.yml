---

kimai_version: 2.0.29

kimai_direct_download: false

kimai_release: {}

kimai_install_dir: /var/www

kimai_owner: www-data
kimai_group: www-data

kimai_database:
  # username: kimai
  # password:
  # hostname:
  # port: 3306
  # schema: kimai
  server:
    version:

kimai_env: prod

kimai_mailer:
  from: ""
  url: "null://null"

kimai_admin_user:
  username: admin
  password: admin0815
  email: admin@foo.bar

kimai_secret: change_this_to_something_unique

...
